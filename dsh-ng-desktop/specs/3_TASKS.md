# 开发任务状态机

本文档记录了项目的开发轨迹，任何对话中的新需求都需要先变为待办事项 (TODO) 落户于此。

## 📅 里程碑 1: 底座搭建与首跑 (M1)

- [ ] **M1.1 初始化工程**: 使用 `dotnet new` 创建 C# 控制台项目，配置为桌面模式（若涉及托盘）并勾选 AOT。
- [ ] **M1.2 极简托盘建设**: 编写纯 C# 原生托盘与右键菜单骨架（目前仅含挂载/退出空壳动作）。
- [ ] **M1.3 配置固化层**: 实现 `Configuration.cs` 与 JSON 的 Source Generator AOT 互操作，探测并锁定启动端口。
- [ ] **M1.4 跨进程基石**: 实现 `DshOrchestrator.cs` 的前置 Node 检查与私有文件夹环境初始化。

## 📅 里程碑 2: DSH 调度与交互强化 (M2)

- [ ] **M2.1 安装流程**: 实现 `npm install deepseek-harness` 派发到指定目录。
- [ ] **M2.2 后台守护运行**: 完整使用无头进程拉起子 dsh 后端，捕获输出监控。
- [ ] **M2.3 WebUI 通路**: 右键菜单的 "打开面板" 调用系统浏览器转至 localhost 端口。
- [ ] **M2.4 系统自启动绑定**: (面向 Windows 的注册表修改)。

## 📅 里程碑 3: 完美谢幕与发布 (M3)

- [ ] **M3.1 无痕卸载 (自焚器)**: 完成 `DshCleaner` 中的关闭进程 -> 删除私有资源 -> Temp 排期删 exe。
- [ ] **M3.2 界面交互细化**: 右键菜单动态状态更新（比如显示端口号、“正在运行”或“环境异常”）。
- [ ] **M3.3 AOT 打包测试**: 完成 `dotnet publish` 并验收本地免环境独立运行和防内存泄露情况。

---
**更新日志**
* 2026-08-15: 重构确立完整的 Spec Coding 文档系，规划初步三步走待办状态。